import{motion}from"framer-motion";import{Button}from"./Button.jsx";export function LogDrawer({log,close}){return <div className="log-drawer-backdrop" onClick={close}><motion.div initial={{x:420,opacity:0}} animate={{x:0,opacity:1}} transition={{type:"spring",stiffness:260,damping:28}} className="log-drawer" onClick={e=>e.stopPropagation()}><div className="log-drawer-head"><h2>Historie</h2><Button onClick={close} variant="secondary">Zavřít</Button></div><div className="log">{log.map((x,i)=><div key={i}>{x}</div>)}</div></motion.div></div>}
